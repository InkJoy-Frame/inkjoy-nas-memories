## 已完成

- 诊断 Docker 日志中两类推送失败的根因：(1) 旧账号 `ray.chen@epaperframe.com` 残留导致 "device does not have permission"；(2) 同一 email 在不同服务器同时触发 schedule 互踢 token 导致 401
- `scheduler_manager.py`：`execute_schedule` 加 per-email 串行锁（`_email_locks = defaultdict(threading.Lock)`），同 email 的 schedule 排队执行，不同 email 互不阻塞。前置校验（schedule 不存在/disabled/账号缺失）在锁外，`try/finally` 保证释放
- 编写并通过 3 个 mock 单元测试验证锁行为：同 email 串行、不同 email 并行、混合场景
- CLAUDE.md 新增"调度器 per-email 串行锁"段落

## 后续建议

1. 用户需在 NAS Web UI 上清理旧账号 `ray.chen@epaperframe.com` 的 schedule 和账号记录——锁只解决并发互踢，不解决已废弃账号的权限错误
2. 两个服务器 URL `open.advisor.00r.top` 和 `openapi.advisor.epaperframe.com` 不在代码 `SERVERS` 字典中，是数据库历史记录——确认哪个是当前有效的，清理另一个的 schedule

## 中断点

无
